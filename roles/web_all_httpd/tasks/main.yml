---
- name: Install Apache httpd 2.4 from RHEL
  yum:
    name: '{{ item }}'
    state: present
  with_items:
    - httpd
    - mod_ssl

- name: Make sure httpd is started at boot.
  service:
    name: httpd
    enabled: true
    state: started

- name: Create django directories
  file:
    name: "/home/apache/.ssh"
    state: directory
    owner: apache
    group: apache
    mode: 0755

- name: Create django directories
  file:
    name: "/home/apache/.ssh/authorized_keys"
    state: file
    owner: apache
    group: apache
    mode: 0600
